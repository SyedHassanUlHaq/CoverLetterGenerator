"""
DEPRECATED: Demo generator script

This file was used as a small demo to build a Mistral prompt from
`data/projects.json`. The functionality has been integrated into the
application (Streamlit UI + `main.generate_proposal`).

Per repository cleanup, this script is intentionally left as a no-op
placeholder. Delete this file manually if you want it removed from
history; otherwise it is safe to ignore.
"""

def main():
    raise SystemExit("generate_demo.py is deprecated — use the Streamlit UI in app.py")


if __name__ == "__main__":
    main()
